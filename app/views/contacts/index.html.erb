<h1>Index page</h1>

<% @contacts.each do |contact| %>
  <h2><a href ="/contacts/<%= contact.id %>"><%= contact.first_name %> <%= contact.last_name %> </a></h2>
  <h3><%= contact.email %></h3>
  <h3><%= contact.phone_number %></h3>
<% end %>

<h1><a href ="/contacts/new">Add a new contact</a></h1>